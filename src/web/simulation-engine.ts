/**
 * Web interface wrapper - directly uses existing ConfigurableSimulation
 * No duplication, just a thin adapter for web-specific needs
 */

export { ConfigurableSimulation as WebSimulationEngine } from '../framework/ConfigurableSimulation'